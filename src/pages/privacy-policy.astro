---

import MainGridLayout from "../layouts/MainGridLayout.astro";

import { getEntry } from "astro:content";
import { render } from "astro:content";
import Markdown from "@components/misc/Markdown.astro";
import I18nKey from "../i18n/i18nKey";
import { i18n } from "../i18n/translation";

const privacyPolicyPost = await getEntry("spec", "privacy-policy");

if (!privacyPolicyPost) {
	throw new Error("privacyPolicy page content not found");
}

const { Content } = await render(privacyPolicyPost);
---
<MainGridLayout title={i18n(I18nKey.privacyPolicy)} description={i18n(I18nKey.privacyPolicy)}>
    <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32">
        <div class="card-base z-10 px-9 py-6 relative w-full ">
            <Markdown class="mt-2">
                <Content />
            </Markdown>
        </div>
    </div>
</MainGridLayout>